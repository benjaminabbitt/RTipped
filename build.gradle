subprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'

    sourceCompatibility = 1.7
    group = 'com.aa.lookAhead.akkaDemo'
    version = '1.0-SNAPSHOT'

    repositories {
        mavenCentral()
        maven { url 'http://repo.akka.io/releases/' }
    }

    dependencies {

    }
}